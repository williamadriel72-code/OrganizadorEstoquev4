t Promise.all([
  sb.from('kh_motoboys').select('*').order('nome'),
  sb.from('kh_motoboy_bairros').select('*').eq('ativo',true).order('nome'),
  sb.from('kh_motoboy_jornadas').select('*').eq('data',t),
  sb.from('kh_motoboy_instalacoes').select('*').order('ultimo_acesso',{ascending:false}).limit(300)
 ]);
 for(const r of [m,b,j,i])if(r.error)throw r.error;
 const jornadas=j.data||[], jids=jornadas.map(x=>x.id);
 let entregas=[],saidas=[];
 if(jids.length){
  const [e,s]=await Promise.all([
   sb.from('kh_motoboy_entregas').select('*').in('jornada_id',jids).order('created_at'),
   sb.from('kh_motoboy_saidas').select('*').in('jornada_id',jids).order('created_at')
  ]);
  if(e.error)throw e.error;if(s.error)throw s.error;entregas=e.data||[];saidas=s.data||[];
 }
 Object.assign(adminState,{motoboys:sortMotos(m.data||[]),bairros:b.data||[],jornadas,instalacoes:i.data||[],entregas,saidas});
 if(adminState.selectedMoto&&!adminState.motoboys.some(x=>x.id===adminState.selectedMoto))adminState.selectedMoto=null;
}
function adminShell(content){return `<div class="admin-layout"><aside class="sidebar"><div class="brand">${logo()}<div><h1>Bora Michael<br>Hi Hi</h1><p>ADMIN</p></div></div><div class="notice" style="margin-top:18px">Clique diretamente em um motoboy para lançar notas e liberar a saída.</div></aside><main class="admin-main"><header class="admin-head"><div><h2>Bora Michael Hi Hi</h2><div class="date">Motoboys em ordem alfabética · lançamento rápido</div></div><button id="logout" class="btn secondary small">Sair</button></header>${content}</main></div>`}
function jornadaOf(mid){return adminState.jornadas.find(x=>x.motoboy_id===mid)}
function entregasOf(mid){return adminState.entregas.filter(x=>x.motoboy_id===mid)}
function saidasOf(mid){return adminState.saidas.filter(x=>x.motoboy_id===mid)}
function dailyBaseOf(j){return j?.chegada_at?Number(j.base_valor||0):0}
function moneyMoto(mid){const j=jornadaOf(mid);return dailyBaseOf(j)+entregasOf(mid).reduce((a,x)=>a+Number(x.valor||0),0)}
function arrivalLabel(j){if(!j?.chegada_at)return `Sem chegada registrada · Diária ${BRL(0)}`;const h=new Date(j.chegada_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});return `${h} · Diária ${BRL(dailyBaseOf(j))}`}
function motoListHtml(){const ms=adminState.motoboys.filter(x=>x.ativo!==false);return `<div class="moto-list"><div class="moto-list-head"><b>MOTOBOYS</b><span class="moto-count">${ms.length} · A → Z</span></div>${ms.map(m=>{const j=jornadaOf(m.id),out=saidasOf(m.id).slice(-1)[0];return `<button class="moto-item ${adminState.selectedMoto===m.id?'active':''}" data-moto-open="${m.id}"><div><div class="moto-item-name">${esc(m.nome)}</div><div class="moto-item-meta">${esc(arrivalLabel(j))}${out?` · Saída ${String(out.numero_sequencial).padStart(2,'0')}`:''}</div></div><div class="moto-item-value">${BRL(moneyMoto(m.id))}</div></button>`}).join('')||'<div class="empty">Nenhum motoboy cadastrado.</div>'}</div>`}
async function ensureJornada(motoboy_id,data=today()){let j=adminState.jornadas.find(x=>x.motoboy_id===motoboy_id&&x.data===data);if(j)return j;let q=await sb.from('kh_motoboy_jornadas').select('*').eq('motoboy_id',motoboy_id).eq('data',data).maybeSingle();if(q.error)throw q.error;if(q.data){adminState.jornadas.push(q.data);return q.data}const r=await sb.from('kh_motoboy_jornadas').insert({motoboy_id,data,chegada_tipo:'nao_informada',base_valor:0}).select().single();if(r.error)throw r.error;adminState.jornadas.push(r.data);return r.data}
function selectedPanel(){const m=adminState.motoboys.find(x=>x.id===adminState.selectedMoto);if(!m)return `<div class="card fast-placeholder"><b>Selecione um motoboy na lista.</b><div class="row-sub" style="margin-top:6px">Ao clicar no nome, o lançamento da saída aparece aqui.</div></div>`;const j=jornadaOf(m.id),es=entregasOf(m.id),pending=es.filter(x=>!x.saida_id&&x.status!=='cancelada'),outs=saidasOf(m.id);const arrival=j?.chegada_at?new Date(j.chegada_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'}):null;return `<div class="moto-detail"><div class="moto-hero"><div><h2>${esc(m.nome)}</h2><div class="row-sub">${arrival?`Chegada ${arrival} · diária fixa ${BRL(dailyBaseOf(j))}`:`Sem chegada registrada · diária ${BRL(0)}`}</div></div><button class="btn small danger" id="deleteMoto">Excluir motoboy</button></div>
 <div class="card"><div class="arrival"><div><div class="stat-label">CHEGADA / DIÁRIA</div>${arrival?`<strong>${arrival} · ${BRL(dailyBaseOf(j))}</strong><div class="row-sub">A diária ficou fixada pelo horário registrado.</div>`:`<strong>${BRL(0)}</strong><div class="row-sub">Sem chegada registrada. A diária fica zerada.</div>`}</div>${arrival?'':`<button id="arrivalNow" class="btn green">REGISTRAR CHEGADA AGORA</button>`}</div>${j?.chegada_at&&j.chegada_tipo==='faixa_manual'&&!Number(j.base_valor)?`<div class="notice" style="margin-top:12px"><b>Chegada entre 18:00 e 18:30.</b> Informe a diária manualmente, pois não há regra automática para essa faixa.</div><div class="quick-actions" style="margin-top:9px"><input id="manualBase" type="number" min="0" step="0.01" placeholder="Valor da diária" style="max-width:190px"><button id="saveManualBase" class="btn gold">Fixar diária</button></div>`:''}</div>
 <div class="card launch-card" style="margin-top:14px"><h3 style="margin-top:0">Nova nota · ${esc(m.nome)}</h3><label class="field"><span>Número da Nota</span><input id="quickNota" inputmode="numeric" autocomplete="off" placeholder="Digite o número da nota"></label><div id="bairroStep" class="step hidden"><label class="field"><span>Bairro</span><input id="bairroBusca" autocomplete="off" placeholder="Digite o nome do bairro"></label><div id="bairroResults" class="bairro-results"></div><div id="bairroSelected"></div><div id="integralQuick" class="step hidden"><label class="field"><span>Valor da entrega integral</span><input id="quickIntegral" type="number" min="0.01" step="0.01" placeholder="R$ 0,00"></label><div class="notice" style="margin-top:7px">Este valor vale somente para esta nota e não vira preço fixo.</div></div><button id="quickAdd" class="big-action" style="margin-top:12px" disabled>+ ADICIONAR NOTA</button></div></div>
 <div class="grid two" style="margin-top:14px"><div class="card"><h3 style="margin-top:0">Aguardando saída</h3><div id="pendingList">${pending.length?pending.map(e=>`<label class="pending-row"><input type="checkbox" data-out-del="${e.id}" checked><div><div class="row-title">Nota ${esc(e.nota_numero)} · ${esc(e.bairro_nome)}</div><div class="row-sub">${e.tipo==='integral'?'<span class="pill integral">Taxa Integral</span>':'Normal'}</div></div><div style="display:flex;gap:7px;align-items:center"><span class="money">${BRL(e.valor)}</span><button type="button" class="btn secondary small" data-edit-del="${e.id}">Alterar</button><button type="button" class="btn danger small" data-delete-del="${e.id}">Excluir</button></div></label>`).join(''):'<div class="empty">Nenhuma entrega aguardando saída.</div>'}</div><button id="releaseSelected" class="btn green" style="width:100%;margi