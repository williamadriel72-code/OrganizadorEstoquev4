today':''}" data-day="${ds}"><b>${d}</b>${v?`<div class="v">${BRL(v)}</div>`:''}</button>`}return `<div class="section-title">Meu calendário</div><div class="card"><div class="month-head"><button id="prevMonth">←</button><b>${rider.month.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</b><button id="nextMonth">→</button></div><div class="calendar">${['SEG','TER','QUA','QUI','SEX','SÁB','DOM'].map(x=>`<div class="dow">${x}</div>`).join('')}${cells}</div></div>`}
async function reportsHtml(){const now=new Date(),y=now.getFullYear(),m=now.getMonth(),md=await monthData(y,m),month=Object.values(md.sums).reduce((a,v)=>a+Number(v||0),0),day=now.getDay(),delta=(day===0?6:day-1),ws=new Date(now);ws.setDate(now.getDate()-delta);ws.setHours(0,0,0,0);let week=0;for(const [ds,v] of Object.entries(md.sums)){const dt=new Date(ds+'T12:00:00');if(dt>=ws&&dt<=now)week+=Number(v||0)}return `<div class="section-title">Meus ganhos</div><div class="reports"><div class="report-card"><div class="stat-label">Esta semana</div><div class="v">${BRL(week)}</div></div><div class="report-card"><div class="stat-label">Este mês</div><div class="v">${BRL(month)}</div></div><div class="notice">Para consultar dias específicos, use o <strong>Calendário</strong>.</div></div>`}
async function riderSyncCheck(force=false,notify=true){if(!rider.profile||rider.syncBusy)return;rider.syncBusy=true;try{const r=await sb.rpc('kh_motoboy_sync_state');if(r.error)throw r.error;const v=String(r.data?.version||'');if(rider.syncVersion===null){rider.syncVersion=v;return}if(force||v!==rider.syncVersion){const changed=v!==rider.syncVersion;rider.syncVersion=v;if(changed&&notify)nativeNotify({source:'sync',version:v});await bmDetectRiderRelease();await renderRider(rider.active||'Hoje')}}catch(e){console.warn('sync',e?.message||e)}finally{rider.syncBusy=false}}
function setupRiderRealtime(){if(!rider.profile)return;const mid=rider.profile.id;if(!rider.channel){let timer,last='';const changed=payload=>{const sig=`${payload.table||''}:${payload.eventType||''}:${payload.new?.id||payload.old?.id||''}:${payload.new?.updated_at||payload.new?.created_at||''}`;if(sig&&sig===last)return;last=sig;if(payload?.table==='kh_motoboy_saidas'&&payload?.eventType==='INSERT')setTimeout(()=>bmPlayRiderReleaseAnimation(payload.new||{}),160);nativeNotify(payload);clearTimeout(timer);timer=setTimeout(async()=>{await riderSyncCheck(true,false)},120)};rider.channel=sb.channel('bm-rider-'+mid).on('postgres_changes',{event:'*',schema:'public',table:'kh_motoboy_entregas',filter:`motoboy_id=eq.${mid}`},changed).on('postgres_changes',{event:'*',schema:'public',table:'kh_motoboy_jornadas',filter:`motoboy_id=eq.${mid}`},changed).on('postgres_changes',{event:'*',schema:'public',table:'kh_motoboy_saidas',filter:`motoboy_id=eq.${mid}`},changed).subscribe(status=>{rider.realtimeStatus=status;if(status==='SUBSCRIBED')riderSyncCheck(false,false)})}if(!rider.pollTimer){rider.pollTimer=setInterval(()=>{if(!document.hidden)riderSyncCheck(false,true)},2000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)riderSyncCheck(false,true)});window.addEventListener('focus',()=>riderSyncCheck(false,true));window.addEventListener('online',()=>riderSyncCheck(false,true));setTimeout(()=>riderSyncCheck(false,false),300)}}


async function boot(){try{const {data:{session}}=await sb.auth.getSession();if(appMode){if(session){rider.profile=await getRiderProfile();if(rider.profile)return renderRider();await sb.auth.signOut()}return registerView()}else{if(session){const test=await sb.from('kh_motoboys').select('id').limit(1);if(!test.error)return renderAdmin();await sb.auth.signOut()}return adminLoginView()}}catch(e){console.error(e);failScreen(e.message)}}
setTimeout(()=>{if($('#root .loading'))failScreen('O carregamento demorou mais do que o esperado.')},12000);
boot();
</script>
</body>
</html>
