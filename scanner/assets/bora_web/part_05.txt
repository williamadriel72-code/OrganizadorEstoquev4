n-top:12px" ${pending.length?'':'disabled'}>LIBERAR SAÍDA</button></div><div class="card"><h3 style="margin-top:0">Saídas de hoje</h3>${outs.length?outs.map(o=>`<div class="row"><div><div class="row-title">Saída ${String(o.numero_sequencial).padStart(2,'0')}</div><div class="row-sub">${new Date(o.horario_saida||o.created_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</div></div><div class="money">${BRL(o.total)}</div></div>`).join(''):'<div class="empty">Nenhuma saída liberada.</div>'}</div></div>
 <div class="card" style="margin-top:14px"><h3 style="margin-top:0">Entregas de hoje</h3>${es.length?es.slice().reverse().map(e=>deliveryRow(e,true)).join(''):'<div class="empty">Nenhuma entrega hoje.</div>'}</div></div>`}
function deliveryRow(e,withEdit=false){return `<div class="row"><div class="row-main"><div class="row-title">Nota ${esc(e.nota_numero)} · ${esc(e.bairro_nome||'Sem bairro')}</div><div class="row-sub">${e.tipo==='integral'?'<span class="pill integral">Taxa Integral</span>':'Taxa normal'} ${e.saida_id?'· em saída':''}</div></div><div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:flex-end"><div class="money">${BRL(e.valor)}</div>${withEdit?`<button class="btn secondary small" data-edit-del="${e.id}">Alterar</button><button class="btn danger small" data-delete-del="${e.id}">Excluir</button>`:''}</div></div>`}
function adminView(){return `<div class="moto-workspace">${motoListHtml()}${selectedPanel()}</div>`}
async function renderAdmin(){try{await loadAdmin();$('#root').innerHTML=adminShell(adminView());bindAdmin()}catch(e){console.error(e);failScreen(e.message)}}
function drawAdmin(){ $('#root').innerHTML=adminShell(adminView());bindAdmin() }
function calcBaseForNow(){const n=new Date(),mins=n.getHours()*60+n.getMinutes(),sun=n.getDay()===0;if(mins<18*60)return {valor:sun?90:70,tipo:'antes_18'};if(mins>=18*60+30)return {valor:sun?80:60,tipo:'apos_1830'};return {valor:null,tipo:'faixa_manual'}}
async function registerArrival(mid){try{if(!mid){toast('Selecione um motoboy.');return}const r=await sb.rpc('kh_register_motoboy_arrival',{p_motoboy_id:mid});if(r.error)throw r.error;const j=r.data;toast(Number(j?.base_valor)>0?`Chegada registrada. Diária ${BRL(j.base_valor)}.`:'Chegada registrada. Defina a diária desta faixa.');await renderAdmin()}catch(e){console.error(e);toast('Não foi possível registrar a chegada. Tente novamente.')}}
function renderBairroResults(q=''){const box=$('#bairroResults');if(!box)return;const term=q.trim().toLocaleLowerCase('pt-BR');let arr=adminState.bairros.filter(b=>!term||String(b.nome).toLocaleLowerCase('pt-BR').includes(term)).slice(0,14);box.innerHTML=arr.map(b=>`<button type="button" class="bairro-option ${adminState.selectedBairro===b.id?'selected':''}" data-bairro-pick="${b.id}"><span><b>${esc(b.nome)}</b><br><small>${b.taxa_integral?'Taxa Integral':'Taxa normal'}</small></span><strong>${b.taxa_integral?'INTEGRAL':BRL(b.valor)}</strong></button>`).join('')||'<div class="empty">Nenhum bairro encontrado.</div>';$$('[data-bairro-pick]').forEach(btn=>btn.onclick=()=>selectBairro(btn.dataset.bairroPick))}
function selectBairro(id){adminState.selectedBairro=id;const b=adminState.bairros.find(x=>x.id===id);if(!b)return;const busca=$('#bairroBusca'),results=$('#bairroResults');if(busca)busca.value=b.nome;if(results){results.innerHTML='';results.style.display='none'}$('#bairroSelected').innerHTML=`<div class="selected-bairro"><b>${esc(b.nome)}</b><div class="row-sub">${b.taxa_integral?'Taxa Integral · informe o valor desta nota':`Valor automático: ${BRL(b.valor)}`}</div></div>`;$('#integralQuick').classList.toggle('hidden',!b.taxa_integral);$('#quickAdd').disabled=false;if(b.taxa_integral)setTimeout(()=>$('#quickIntegral')?.focus(),20)}
async function quickAddDelivery(){const mid=adminState.selectedMoto,nota=$('#quickNota').value.trim(),b=adminState.bairros.find(x=>x.id===adminState.selectedBairro);if(!mid||!nota||!b){toast('Informe a nota e o bairro.');return}let valor=b.taxa_integral?Number($('#quickIntegral').value):Number(b.valor);if(!Number.isFinite(valor)||valor<=0){toast(b.taxa_integral?'Digite o valor da entrega integral.':'Bairro sem valor válido.');return}try{const j=await ensureJornada(mid);const now=new Date().toISOString(),r=await sb.from('kh_motoboy_entregas').insert({jornada_id:j.id,motoboy_id:mid,bairro_id:b.id,bairro_nome:b.nome,tipo:b.taxa_integral?'integral':'normal',valor,valor_notinha:b.taxa_integral?valor:null,nota_numero:nota,status:'lancada',updated_at:now}).select().single();if(r.error)throw r.error;toast('Nota adicionada.');adminState.selectedBairro=null;await renderAdmin();setTimeout(()=>$('#quickNota')?.focus(),30)}catch(e){toast(e.message||'Não foi possível adicionar a nota.')}}
const BM_DISPATCH_RIDER_ASSET='https://raw.githubusercontent.com/williamadriel72-code/OrganizadorEstoquev4/704240bbf7e256e8ffd9cf16b90f065e0b1a80b9/scanner/assets/bora_web/motoboy-saida-mobile.webp';
function bmEnsureDispatchStyles(){if(document.getElementById('bmDispatchAnimationStyles'))return;const s=document.createElement('style');s.id='bmDispatchAnimationStyles';s.textContent=`
.bm-dispatch-overlay{position:fixed;inset:0;z-index:99999;overflow:hidden;pointer-events:none;isolation:isolate}
.bm-dispatch-shade{position:absolute;inset:0;background:radial-gradient(circle at 50% 55%,rgba(242,163,60,.14),rgba(12,14,16,.08) 52%,transparent 76%);opacity:0}
.bm-dispatch-copy{position:absolute;z-index:3;left:50%;top:max(16px,env(safe-area-inset-top));width:min(92vw,420px);transform:translate3d(-50%,-14px,0);padding:12px 15px;border:1px solid rgba(242,163,60,.6);border-radius:16px;background:linear-gradient(135deg,rgba(26,29,33,.98),rgba(12,14,16,.95));box-shadow:0 16px 34px rgba(0,0,0,.38);text-align:center;opacity:0}
.bm-dispatch-kicker{color:#f2a33c;font-size:10px;font-weight:900;letter-spacing:1.3px}.bm-dispatch-title{margin-top:3px;color:#fff;font-size:16px;font-weight:900}.bm-dispatch-meta{margin-top:3px;color:#c2c7cc;font-size:12px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.bm-dispatch-bike{position:absolute;z-index:2;left:0;top:55%;width:clamp(250px,38vw,440px);transform:translate3d(-120%,-50%,0);opacity:0;filter:drop-shadow(0 18px 14px rgba(0,0,0,.48))}.bm-dispatch-bike img{display:block;width:100%;height:auto}.bm-dispatch-fallback{display:none;place-items:center;width:92px;height:92px;border-radius:50%;background:#171b1f;border:1px solid #f2a33c;color:#fff;font-size:45px;box-shadow:0 14px 26px #0008}
.bm-dispatch-no-image .bm-dispatch-bike img{display:none}.bm-dispatch-no-image .bm-dispatch-fallback{display:grid}
.bm-dispatch-run .bm-dispatch-shade{animation:bmDispatchShade 2.3s ease both}.bm-dispatch-run .bm-dispatch-copy{animation:bmDispatchCopy 2.1s cubic-bezier(.2,.8,.2,1) both}.bm-dispatch-run.bm-dispatch-admin .bm-dispatch-bike{animation:bmDispatchAdminRide 1.85s cubic-bezier(.18,.75,.18,1) both}.bm-dispatch-run.bm-dispatch-rider .bm-dispatch-bike{animation:bmDispatchRiderRide 3.15s cubic-bezier(.18,.75,.18,1) both}.bm-dispatch-finish{opacity:0;transition:opacity .18s ease}
@keyframes bmDispatchShade{0%{opacity:0}14%{opacity:1}82%{opacity:1}100%{opacity:0}}@keyframes bmDispatchCopy{0%{opacity:0;transform:translate3d(-50%,-14px,0)}12%{opacity:1;transform:translate3d(-50%,0,0)}75%{opacity:1;transform:translate3d(-50%,0,0)}100%{opacity:0;transform:translate3d(-50%,-8px,0)}}@keyframes bmDispatchAdminRide{0%{opacity:0;transform:translate3d(22vw,-50%,0) scale(.9)}10%{opacity:1}76%{opacity:1;transform:translate3d(78vw,-50%,0) scale(1)}100%{opacity:0;transform:translate3d(125vw,-50%,0) scale(.96)}}@keyframes bmDispatchRiderRide{0%{opacity:0;transform:translate3d(-120%,-50%,0) scale(.9)}12%{opacity:1}38%{transform:translate3d(20vw,-50%,0) scale(1)}60%{opacity:1;transform:translate3d(29vw,-50%,0) scale(1)}100%{opacity:0;transform:translate3d(125vw,-50%,0) scale(.97)}}
@media(max-width:800px){.bm-dispatch-bike{width:min(86vw,380px);top:57%}.bm-dispatch-copy{top:max(12px,env(safe-area-inset-top));padding:11px 13px}.bm-dispatch-title{font-size:15px}@keyframes bmDispatchAdminRide{0%{opacity:0;transform:translate3d(10vw,-50%,0) scale(.9)}10%{opacity:1}76%{opacity:1;transform:translate3d(62vw,-50%,0) scale(1)}100%{opacity:0;transform:translate3d(118vw,-50%,0) scale(.96)}}@keyframes bmDispatchRiderRide{0%{opacity:0;transform:translate3d(-120%,-50%,0) scale(.9)}12%{opacity:1}38%{transform:translate3d(7vw,-50%,0) scale(1)}60%{opacity:1;transform:translate3d(14vw,-50%,0) scale(1)}100%{opacity:0;transform:translate3d(118vw,-50%,0) scale(.97)}}}
@media(prefers-reduced-motion:reduce){.bm-dispatch-overlay{display:none!important}}
`;document.head.appendChild(s);const preload=document.createElement('link');preload.id='bmDispatchAssetPreload';preload.rel='preload';preload.as='image';preload.href=BM_DISPATCH_RIDER_ASSET;document.head.appendChild(preload)}
function bmDispatchNoteLabel(notes){const arr=[...(notes||[])].filter(Boolean).map(String);if(!arr.length)return 'Pedido liberado';const shown=arr.slice(0,3).map(esc).join(' · ');return `Comanda${arr.length===1?'':'s'} ${shown}${arr.length>3?` +${arr.length-3}`:''}`}
function bmPlayDispatchAnimation({mode='admin',riderName='',outingNumber='',notes=[]}={}){if(!document.body||window.matchMedia?.('(prefers-reduced-motion: reduce)').matches)return;bmEnsureDispatchStyles();document.getElementById('bmDispatchAnimation')?.remove();const riderMode=mode==='rider'?'rider':'admin',number=Number(outingNumber)||0,title=riderMode==='admin'?(riderName?`Saída liberada · ${esc(riderName)}`:'Saída liberada'):(number?`Sua saída ${String(number).padStart(2,'0')} foi liberada`:'Sua saída foi liberada'),meta=number?`Saída ${String(number).padStart(2,'0')} · ${bmDispatchNoteLabel(notes)}`:bmDispatchNoteLabel(notes),el=document.createElement('section');el.id='bmDispatchAnimation';el.className=`bm-dispatch-overlay bm-dispatch-${riderMode}`;el.setAttribute('aria-hidden','true');el.innerHTML=`<div class="bm-dispatch-shade"></div><div class="bm-dispatch-copy"><div class="bm-dispatch-kicker">BORA MICHAEL HI HI</div><div class="bm-dispatch-title">${title}</div><div class="bm-dispatch-meta">${meta}</div></div><div class="bm-dispatch-bike"><img src="${BM_DISPATCH_RIDER_ASSET}" alt=""><span class="bm-dispatch-fallback">🏍️</span></div>`;document.body.appendChild(el);el.querySelector('img')?.addEventListener('error',()=>el.classList.add('bm-dispatch-no-image'),{once:true});requestAnimationFrame(()=>el.classList.add('bm-dispatch-run'));const duration=riderMode==='rider'?3250:1950;setTimeout(()=>{if(!el.isConnected)return;el.classList.add('bm-dispatch-finish');setTimeout(()=>el.remove(),220)},duration)}
async function bmPlayRiderReleaseAnimation(outing){if(document.visibilityState!=='visible')return;const id=String(outing?.id||'');if(id&&rider.lastDispatchAnimationId===id)return;if(id)rider.lastDispatchAnimationId=id;let notes=[];try{const d=await riderDay(today());notes=(d.e||[]).filter(x=>x.saida_id===outing?.id&&x.status!=='cancelada').map(x=>x.nota_numero)}catch(e){console.warn('dispatch-animation-notes',e?.message||e)}bmPlayDispatchAnimation({mode:'rider',outingNumber:outing?.numero_sequencial,notes})}
async function releaseSelected(){const mid=adminState.selectedMoto,ids=$$('[data-out-del]:checked').map(x=>x.dataset.outDel);if(!mid||!ids.length){toast('Marque pelo menos uma entrega.');return}const btn=$('#releaseSelected');if(btn){btn.disabled=true;btn.textContent='LIBERANDO...'}try{const moto=adminState.motoboys.find(x=>x.id===mid),notes=ids.map(id=>adminState.entregas.find(x=>x.id===id)?.nota_numero).filter(Boolean),r=await sb.rpc('kh_release_motoboy_outing',{p_motoboy_id:mid,p_delivery_ids:ids});if(r.error)throw r.error;const when=r.data?.horario_saida||new Date().toISOString();toast(`Saída liberada às ${new Date(when).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}.`);await renderAdmin();bmPlayDispatchAnimation({mode:'admin',riderName:moto?.nome||'',outingNumber:r.data?.numero_sequencial,notes})}catch(e){console.error(e);toast(e?.message?`Não foi possível liberar a saída: ${e.message}`:'Não foi possível liberar a saída.');if(btn){btn.disabled=false;btn.textContent='LIBERAR SAÍDA'}}}
function modal(html){const el=document.createElement('div');el.className='modal';el.id='modal';el.innerHTML=`<div class="modal-card">${html}</div>`;document.body.appendChild(el);el.querySelector('[data-close]')?.addEventListener('click',()=>el.remove());return el}
function openEditDelivery(id){const e=adminState.entregas.find(x=>x.id===id);if(!e)return;const el=modal(`<div class="modal-head"><h3>Editar entrega</h3><button class="x" data-close>×</button></div><label class="field"><span>Número da nota</span><input id="eNota" value="${esc(e.nota_numero)}"></label><label class="field" style="margin-top:10px"><span>Bairro</span><select id="eBairro">${adminState.bairros.map(x=>`<option value="${x.id}" ${x.id===e.bairro_id?'selected':''}>${esc(x.nome)}${x.taxa_integral?' · Integral':' · '+BRL(x.valor)}</option>`).join('')}</select></label><div id="eIntegralBox" class="field" style="margin-top:10px"><span>Valor Integral</span><input id="eIntegral" type="number" step="0.01" min="0.01" value="${e.tipo==='integral'?Number(e.valor||0):''}"></div><div id="eInfo" class="notice" style="margin-top:10px"></div><button id="saveEditDelivery" class="btn gold" style="width:100%;margin-top:14px">Salvar alteração</button>`);const sync=()=>{const b=adminState.bairros.find(x=>x.id===$('#eBairro').value);$('#eIntegralBox').classList.toggle('hidden',!b?.taxa_integral);$('#eInfo').innerHTML=b?.taxa_integral?'<b>Taxa Integral:</b> este valor pertence 