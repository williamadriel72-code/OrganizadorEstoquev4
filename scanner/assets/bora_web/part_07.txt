erAgent.slice(0,120),app_version:'2.4.0'})});const j=await r.json();if(!r.ok)throw Error(j.error||'Não foi possível registrar este aparelho.');const ss=await sb.auth.setSession({access_token:j.access_token,refresh_token:j.refresh_token});if(ss.error)throw ss.error;rider.profile=j.profile;renderRider('Hoje')}catch(e){m.textContent=e.message||'Não foi possível registrar este aparelho agora.'}};
}
async function getRiderProfile(){const r=await sb.from('kh_motoboys').select('id,nome,ativo').eq('ativo',true).maybeSingle();if(r.error)throw r.error;return r.data}
async function riderDay(date){const j=await sb.from('kh_motoboy_jornadas').select('id,data,base_valor,chegada_at,chegada_tipo,fechado').eq('motoboy_id',rider.profile.id).eq('data',date).maybeSingle();if(j.error)throw j.error;if(!j.data)return {j:null,e:[],s:[],total:0};const [e,so]=await Promise.all([sb.from('kh_motoboy_entregas').select('id,nota_numero,bairro_nome,tipo,valor,status,saida_id,created_at,updated_at').eq('jornada_id',j.data.id).order('created_at'),sb.from('kh_motoboy_saidas').select('id,numero_sequencial,horario_saida,total,status,created_at').eq('jornada_id',j.data.id).order('numero_sequencial')]);if(e.error)throw e.error;if(so.error)throw so.error;const total=(j.data.chegada_at?Number(j.data.base_valor||0):0)+(e.data||[]).reduce((a,x)=>a+Number(x.valor||0),0);return {j:j.data,e:e.data||[],s:so.data||[],total}}
function navRider(active){return `<nav class="nav">${[['Hoje','●'],['Calendário','▦'],['Histórico','≡'],['Relatórios','Σ'],['Taxas','R$']].map(([n,i])=>`<button data-rnav="${n}" class="${active===n?'active':''}"><b>${i}</b>${n}</button>`).join('')}</nav>`}
function riderHeader(){return `<header class="top"><div class="brand">${logo()}<div><h1>Olá, ${esc(rider.profile.nome)}</h1><p>Seus ganhos</p><div class="date">${new Date().toLocaleDateString('pt-BR')}</div></div></div></header>`}
function todayHtml(d){return `<section class="rider-fast-panel"><div class="today-summary"><div class="card"><div class="stat-label">ENTREGAS</div><div class="stat-value">${d.e.length}</div></div><div class="card"><div class="stat-label">TOTAL DO DIA</div><div class="stat-value">${BRL(d.total)}</div></div></div>${d.j?.chegada_at?`<div class="notice" style="margin-top:10px">Chegada registrada às <b>${new Date(d.j.chegada_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</b> · diária ${BRL(d.j.base_valor||0)}</div>`:''}<div class="section-title">Entregas de hoje</div><div class="card">${d.e.length?d.e.map(e=>`<div class="row"><div><div class="row-title">Nota ${esc(e.nota_numero)} · ${esc(e.bairro_nome)}</div><div class="row-sub">${e.tipo==='integral'?'<span class="pill integral">Taxa Integral</span>':'Taxa normal'}</div></div><div class="money">${BRL(e.valor)}</div></div>`).join(''):'<div class="empty">Nenhuma entrega lançada hoje.</div>'}</div></section>`}
async function renderRider(active='Hoje'){
 try{rider.active=active;if(!rider.profile)rider.profile=await getRiderProfile();if(!rider.profile){await sb.auth.signOut();return registerView()}let content='';if(active==='Hoje'){const d=await riderDay(today());rider.todayCache=d;bmPrimeRiderDispatches(d.s);content=todayHtml(d)}else if(active==='Calendário'){content=await calendarHtml()}else if(active==='Histórico'){content=`<div class="section-title">${new Date(rider.selected+'T12:00:00').toLocaleDateString('pt-BR')}</div>${dayHistoryHtml(await riderDay(rider.selected))}`}else if(active==='Relatórios'){content=await reportsHtml()}else{content=`<div class="section-title">Taxas e diária</div><div class="reports"><div class="report-card"><b>Segunda a sábado</b><div class="row-sub" style="margin-top:8px">Antes das 18:00 — R$ 70,00</div><div class="row-sub">Após as 18:30 — R$ 60,00</div></div><div class="report-card"><b>Domingo</b><div class="row-sub" style="margin-top:8px">Antes das 18:00 — R$ 90,00</div><div class="row-sub">Após as 18:30 — R$ 80,00</div></div><div class="notice"><strong>Taxa Integral:</strong> o valor muda conforme a nota e nunca vira preço fixo do bairro.</div></div>`}$('#root').innerHTML=`<div class="shell">${riderHeader()}${content}</div>${navRider(active)}`;$$('[data-rnav]').forEach(b=>b.onclick=()=>renderRider(b.dataset.rnav));$$('[data-day]').forEach(b=>b.onclick=()=>{rider.selected=b.dataset.day;renderRider('Histórico')});$('#prevMonth')?.addEventListener('click',()=>{rider.month=new Date(rider.month.getFullYear(),rider.month.getMonth()-1,1);renderRider('Calendário')});$('#nextMonth')?.addEventListener('click',()=>{rider.month=new Date(rider.month.getFullYear(),rider.month.getMonth()+1,1);renderRider('Calendário')});setupRiderRealtime();appReady()}catch(e){console.error(e);failScreen(e.message)}}
function dayHistoryHtml(d){if(!d.j)return '<div class="card"><div class="empty">Sem entregas neste dia.</div></div>';return `<div class="grid two"><div class="card"><div class="stat-label">Base do dia</div><div class="stat-value">${BRL(d.j.chegada_at?d.j.base_valor:0)}</div></div><div class="card"><div class="stat-label">Total</div><div class="stat-value">${BRL(d.total)}</div></div></div><div class="section-title">Entregas</div><div class="card">${d.e.map(e=>`<div class="row"><div><div class="row-title">Nota ${esc(e.nota_numero)} · ${esc(e.bairro_nome)}</div><div class="row-sub">${e.tipo==='integral'?'Taxa Integral':'Normal'}</div></div><div class="money">${BRL(e.valor)}</div></div>`).join('')||'<div class="empty">Nenhuma entrega.</div>'}</div><div class="section-title">Saídas</div><div class="card">${d.s.map(s=>`<div class="row"><div><div class="row-title">Saída ${String(s.numero_sequencial).padStart(2,'0')}</div><div class="row-sub">${new Date(s.horario_saida||s.created_at).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</div></div><div class="money">${BRL(s.total)}</div></div>`).join('')||'<div class="empty">Nenhuma saída registrada.</div>'}</div>`}
async function monthData(y,m){const start=`${y}-${String(m+1).padStart(2,'0')}-01`,end=isoDate(new Date(y,m+1,0));const j=await sb.from('kh_motoboy_jornadas').select('id,data,base_valor,chegada_at').eq('motoboy_id',rider.profile.id).gte('data',start).lte('data',end);if(j.error)throw j.error;const ids=(j.data||[]).map(x=>x.id);let es=[];if(ids.length){const e=await sb.from('kh_motoboy_entregas').select('jornada_id,valor').in('jornada_id',ids);if(e.error)throw e.error;es=e.data||[]}const jmap=new Map((j.data||[]).map(x=>[x.id,x.data])),sums={};for(const x of j.data||[])sums[x.data]=x.chegada_at?Number(x.base_valor||0):0;for(const e of es){const d=jmap.get(e.jornada_id);if(d)sums[d]=(sums[d]||0)+Number(e.valor||0)}return {j:j.data||[],e:es,sums}}
async function calendarHtml(){const y=rider.month.getFullYear(),m=rider.month.getMonth(),md=await monthData(y,m),first=new Date(y,m,1),last=new Date(y,m+1,0).getDate(),offset=(first.getDay()+6)%7;let cells='';for(let i=0;i<offset;i++)cells+='<div></div>';for(let d=1;d<=last;d++){const ds=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`,v=md.sums[ds]||0;cells+=`<button class="day ${v?'worked':''} ${ds===today()?'