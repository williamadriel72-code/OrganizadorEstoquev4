somente a esta nota.':`Valor automático do bairro: <b>${BRL(b?.valor)}</b>`};$('#eBairro').onchange=sync;sync();$('#saveEditDelivery').onclick=async()=>{const b=adminState.bairros.find(x=>x.id===$('#eBairro').value),nota=$('#eNota').value.trim(),valor=b?.taxa_integral?Number($('#eIntegral').value):Number(b?.valor);if(!b||!nota||!Number.isFinite(valor)||valor<=0){toast('Confira os dados.');return}const r=await sb.from('kh_motoboy_entregas').update({nota_numero:nota,bairro_id:b.id,bairro_nome:b.nome,tipo:b.taxa_integral?'integral':'normal',valor,valor_notinha:b.taxa_integral?valor:null,updated_at:new Date().toISOString()}).eq('id',e.id);if(r.error){toast(r.error.message);return}if(e.saida_id){const rr=await sb.rpc('kh_recalculate_outing',{p_saida_id:e.saida_id});if(rr.error){toast(rr.error.message);return}}el.remove();toast('Entrega atualizada.');await renderAdmin()}}
function confirmDeleteDelivery(id){const e=adminState.entregas.find(x=>x.id===id);if(!e)return;const el=modal(`<div class="modal-head"><h3>Excluir pedido?</h3><button class="x" data-close>×</button></div><div class="notice"><b>Nota ${esc(e.nota_numero)} · ${esc(e.bairro_nome||'Sem bairro')}</b><br>Valor ${BRL(e.valor)}${e.saida_id?'<br><br>Este pedido já está em uma saída. O total da saída será recalculado automaticamente.':''}</div><div class="quick-actions" style="margin-top:14px"><button class="btn secondary" data-close>Cancelar</button><button id="deleteDeliveryNow" class="btn danger">EXCLUIR PEDIDO</button></div>`);$('#deleteDeliveryNow').onclick=async()=>{try{const r=await sb.rpc('kh_delete_motoboy_delivery',{p_delivery_id:e.id});if(r.error)throw r.error;el.remove();toast('Pedido excluído.');await renderAdmin()}catch(err){console.error(err);toast(err.message||'Não foi possível excluir o pedido.')}}}
function confirmDeleteMoto(){const m=adminState.motoboys.find(x=>x.id===adminState.selectedMoto);if(!m)return;const el=modal(`<div class="modal-head"><h3>Excluir ${esc(m.nome)} permanentemente?</h3><button class="x" data-close>×</button></div><div class="notice danger"><b>Esta ação excluirá tudo.</b><br>Cadastro, aparelhos, jornadas, entregas, Taxas Integrais, saídas e histórico financeiro deste motoboy.</div><div class="delete-note">Para confirmar, digite <b>EXCLUIR</b>.</div><input id="deleteConfirm" style="margin-top:10px" placeholder="EXCLUIR"><div class="quick-actions" style="margin-top:14px"><button class="btn secondary" data-close>Cancelar</button><button id="deleteAllMoto" class="btn danger">EXCLUIR TUDO</button></div>`);$('#deleteAllMoto').onclick=async()=>{if($('#deleteConfirm').value.trim().toUpperCase()!=='EXCLUIR'){toast('Digite EXCLUIR para confirmar.');return}try{const {data:{session}}=await sb.auth.getSession();if(!session)throw Error('Sessão expirada.');const r=await fetch(U+'/functions/v1/bhhi-admin-actions',{method:'POST',headers:{'Content-Type':'application/json',apikey:K,Authorization:'Bearer '+session.access_token},body:JSON.stringify({action:'delete_motoboy',motoboy_id:m.id})});const j=await r.json();if(!r.ok)throw Error(j.error||'Não foi possível excluir.');el.remove();adminState.selectedMoto=null;toast('Motoboy e todo o histórico foram excluídos.');await renderAdmin()}catch(e){toast(e.message)}}}
function bindAdmin(){
 $('#logout').onclick=async()=>{await sb.auth.signOut();adminLoginView()};
 $$('[data-moto-open]').forEach(b=>b.onclick=()=>{adminState.selectedMoto=b.dataset.motoOpen;adminState.selectedBairro=null;drawAdmin();setTimeout(()=>$('#quickNota')?.focus(),30)});
 $('#arrivalNow')?.addEventListener('click',()=>registerArrival(adminState.selectedMoto));
 $('#saveManualBase')?.addEventListener('click',async()=>{const v=Number($('#manualBase').value),j=jornadaOf(adminState.selectedMoto);if(!j||!Number.isFinite(v)||v<=0){toast('Informe um valor válido.');return}const r=await sb.from('kh_motoboy_jornadas').update({base_valor:v,updated_at:new Date().toISOString()}).eq('id',j.id);if(r.error){toast(r.error.message);return}toast('Diária fixada.');await renderAdmin()});
 const notaInput=$('#quickNota');
 const abrirBairro=()=>{if(!notaInput?.value.trim())return;$('#bairroStep').classList.remove('hidden');const busca=$('#bairroBusca'),results=$('#bairroResults');if(results)results.style.display='grid';renderBairroResults(busca?.value||'')};
 notaInput?.addEventListener('input',()=>{adminState.selectedBairro=null;$('#bairroStep')?.classList.add('hidden');$('#bairroSelected').innerHTML='';$('#integralQuick')?.classList.add('hidden');$('#quickAdd').disabled=true});
 notaInput?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();abrirBairro();setTimeout(()=>$('#bairroBusca')?.focus(),20)}});
 notaInput?.addEventListener('blur',()=>{if(notaInput.value.trim())abrirBairro()});
 $('#bairroBusca')?.addEventListener('focus',e=>{const results=$('#bairroResults');if(results)results.style.display='grid';renderBairroResults(e.target.value)});
 $('#bairroBusca')?.addEventListener('input',e=>{adminState.selectedBairro=null;$('#bairroSelected').innerHTML='';$('#integralQuick')?.classList.add('hidden');$('#quickAdd').disabled=true;const results=$('#bairroResults');if(results)results.style.display='grid';renderBairroResults(e.target.value)});
 $('#quickAdd')?.addEventListener('click',quickAddDelivery);
 $('#releaseSelected')?.addEventListener('click',releaseSelected);
 $$('[data-edit-del]').forEach(b=>b.onclick=e=>{e.preventDefault();e.stopPropagation();openEditDelivery(b.dataset.editDel)});
 $$('[data-delete-del]').forEach(b=>b.onclick=e=>{e.preventDefault();e.stopPropagation();confirmDeleteDelivery(b.dataset.deleteDel)});
 $('#deleteMoto')?.addEventListener('click',confirmDeleteMoto);
}


let rider={profile:null,month:new Date(),selected:today(),channel:null,active:'Hoje',todayCache:null,pollTimer:null,syncVersion:null,syncBusy:false,realtimeStatus:null};
async function registerView(){
 $('#root').innerHTML=`<section class="login"><div class="login-card"><div class="brand">${logo()}<div><h1>Bora Michael Hi Hi</h1><p>MOTOBOY</p></div></div><h2>Identifique este aparelho</h2><div class="notice">Na primeira abertura, informe somente seu nome. Este aparelho ficará registrado automaticamente.</div><label class="field" style="margin-top:14px"><span>Seu nome</span><input id="rName" placeholder="Digite seu nome"></label><button id="register" class="btn green" style="width:100%;margin-top:14px">Registrar este aparelho</button><div id="rmsg" class="msg"></div></div></section>`;
 $('#register').onclick=async()=>{const nome=$('#rName').value.trim(),m=$('#rmsg');if(nome.length<2){m.textContent='Digite seu nome.';return}m.textContent='Registrando...';let iid=localStorage.getItem('bora-installation-id');if(!iid){iid='bm-'+crypto.randomUUID();localStorage.setItem('bora-installation-id',iid)}try{const r=await fetch(U+'/functions/v1/bhhi-device',{method:'POST',headers:{'Content-Type':'application/json',apikey:K},body:JSON.stringify({action:'register',installation_uuid:iid,nome,dispositivo:navigator.us